import 'package:flutter/material.dart';

class BasicProfileTab extends StatelessWidget {
  const BasicProfileTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
